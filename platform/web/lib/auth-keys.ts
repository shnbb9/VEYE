/** sessionStorage keys that carry the onboarding result gate into sign-up
 *  (per-tab, cleared once the account exists; never sent anywhere else). */
export const PREFILL_EMAIL_KEY = "veye_prefill_email";
export const PENDING_ANSWERS_KEY = "veye_pending_answers";
