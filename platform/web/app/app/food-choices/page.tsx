import { HoldingScreen } from "@/components/member/holding-screen";

export default function FoodChoicesPage() {
  return <HoldingScreen title="Food Choices" icon="food-choices"
    reason="The Food Choices decision tree and business logic have not been supplied by the client yet, so nothing here is invented." />;
}
