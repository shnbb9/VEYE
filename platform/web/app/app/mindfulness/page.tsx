import { HoldingScreen } from "@/components/member/holding-screen";

export default function MindfulnessPage() {
  return <HoldingScreen title="Mindfulness" icon="mindfulness"
    reason="No Mindfulness content has been supplied yet; the section opens the moment the client provides it." />;
}
