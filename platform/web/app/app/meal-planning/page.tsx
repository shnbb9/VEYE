import { HoldingScreen } from "@/components/member/holding-screen";

export default function MealPlanningPage() {
  return <HoldingScreen title="Meal Planning" icon="meal-planning"
    reason="Meal Planning depends on the HOW TO / AI meal-planning specification the client is still preparing; no plan is generated until it is defined." />;
}
