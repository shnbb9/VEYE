"""GuidedFlow: Cara's deterministic decision trees as versioned, persisted
guided experiences that Sprout runs for members.

The engine decides the step, the branches and the navigation; the language
model (optional) only interprets free text into one of the step's allowed
choices and answers explanatory questions through the existing Companion
pipeline while the flow state waits."""
